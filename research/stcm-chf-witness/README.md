# STCM/CHF × thermodynamic-witness inert comparison (source-only)

Canonical Goal: `STCM-CHF-THERMODYNAMIC-WITNESS-COMPARISON-001` · COSV `10111010112000` · [coordination #79](https://github.com/Admissible-Existence/.github/issues/79).

This folder is **synthetic validation material, not a third-party technical export or real experiment**. Frozen synthetic source bytes are `b'{"synthetic":"source-v0"}\\n'`, SHA-256 `fa95f04d35e51df5892a35dc2dd28e823696334082320c60df29d06c50bead02`. The fixture generator is not a runtime executor. Validator output `INERT_*` never grants authority or proves source-native mathematical execution; `DENY_*` and `UNKNOWN_*` are bounded synthetic decisions.

Schemas distinguish exact frozen specimen/source bytes, causally witnessed transitions, independently attributable observation and Landauer lower-bound **calculation** versus observed physical dissipation. The synthetic negative controls cover digest mismatch, missing predecessor/causal proof, timestamp coincidence, calculated-only heat and false witness custody. Source-only CI does not establish external collaborator permission, provider measurements, native STCM/CHF execution, SDK manifest invocation, WorkerCoordinator claim/fence, Interlock/InTr admission or Master Records custody.

To run locally with `jsonschema` installed: `cd research/stcm-chf-witness && python -m unittest -v test_inert_comparison.py`. Historical generation-214 preparation artifacts remain archival, never admissible current-generation check-in requests. A real comparative test requires separately approved exact third-party bytes and source-native independently run receipt chains.

## Independent pinned native-source baselines

`native-source-validation-contract.json` pins an exact STCM revision and CHF revision and selected exact Git blobs. `validate_pinned_native_sources.py` runs the existing public STCM nine-layer closure harness from its exact pinned checkout; the six CHF native validators run in the existing private CHF repository's own CI; the GitHub validation-only workflow retains original stdout/stderr hashes, pinned revision/blob evidence and native result classification. A passing baseline establishes only that each independently pinned source implementation executed its *own* tests. It does **not** establish shared-specimen equivalence, an SDK-manifest invocation, source-native outputs for the synthetic fixture, independently witnessed external measurements or governed execution. Failures are retained as actual native-source test failures and routed to the owning native repository without silently editing either mathematical formalism.

**Verified separate owner CI:** STCM pinned baseline run [36086053793](https://github.com/Admissible-Existence/.github/actions/runs/36086053793) succeeded with an exact-source validation artifact. The original private CHF source was not copied into this public repository: CHF [PR #4](https://github.com/Admissible-Existence/CHF/pull/4) was merged after native CHF six-checker run [36086125184](https://github.com/Admissible-Existence/CHF/actions/runs/36086125184) and Tri-Form run 36086125211 passed. This cross-repository correlation provides **independent native baseline evidence**, not a joint common-specimen test, native CHF numeric witness function, or an authenticated resident/SDK execution receipt.

## Frozen-specimen source-native STCM probe

`probe_frozen_stcm_native.py` invokes the **unchanged pinned** original STCM `gated_close` with the same frozen synthetic specimen as comparison PR #81. Its positive path **constructs a synthetic test predecessor explicitly** solely to exercise `BOUND` and the source closure predicate; it is not a retained or authenticated receipt. Its adversarial paths refuse missing actual predecessors and coincident timestamps, and reject mismatched original specimen bytes before native invocation. `same-specimen-capability-map.json` records the exact original STCM callable and documents why CHF's six current source validators are **not** a per-specimen consequence-horizon numerical processor. The existing SDK also lacks a demonstrated installed generic native mathematical binding. Consequently a two-framework same-specimen execution is **NOT_ESTABLISHED**, regardless of this one-sided native positive/negative source test. Original CHF mathematics and SDK binding remain their independent owners; no duplicative numerical rule or runtime is created.

## Original-source CHF four-threshold execution on the same frozen synthetic source

The private original CHF owner now has [merged source PR #5](https://github.com/Admissible-Existence/CHF/pull/5), merge `27b42eb19f8adc2dd2ee3d1130045a6c5d15a19a`. Its new **bounded** original native `tools/evaluate_chf_specimen_thresholds.py::evaluate_specimen` compares externally supplied R/O/A/K values against externally supplied minimum thresholds from the *existing* four CHF symbolic inequalities; it never manufactures physical measurements or a complete consequence-horizon verdict. [Original-owner hosted validation run 36095046140](https://github.com/Admissible-Existence/CHF/actions/runs/36095046140) and original Tri-Form run 36095046224 both PASSED. [Retained native four-case artifact](https://github.com/Admissible-Existence/CHF/actions/runs/36095046140/artifacts/10847107096), ZIP SHA-256 `7d3db6a52e1af1716894de8e75bbfb223496d8fa96da8275483531c01b2a4084`, binds the exact `fa95f04d...bead02` frozen synthetic source bytes and distinguishes threshold pass, absorption threshold violation, missing field UNKNOWN and forged original bytes DENY. Its 14 original-source tests also passed. The positive numeric values and thresholds were synthetic, not a claim of physical recoverability or observed heat.

STCM [merged PR #83](https://github.com/Admissible-Existence/.github/pull/83) separately ran the **same immutable frozen synthetic source bytes** through original pinned `lineage_gate.gated_close` and retained a distinct original native artifact `10845258620` with synthetically constructed predecessor and adversarial cases. The machine-readable `same-specimen-capability-map.json` binds both original-owner PRs, source SHA and retained artifact hashes without importing private CHF source or inventing a common runtime. These are **two different bounded evaluations of one synthetic source**, not full STCM/CHF theoretical equivalence, physically observed thermodynamics, independently witnessed causal custody or a published SDK native mathematical route. Actual external permission/original technical export, genuine resident execution, full CHF horizon semantics and applicable Master Records closure remain separate unobserved predicates.
